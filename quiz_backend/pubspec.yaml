name: quiz_backend
description: A server app using the shelf package and Docker.
version: 1.0.0
publish_to: none
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  args: ^2.0.0
  shelf: ^1.1.0
  shelf_router: ^1.0.0
  quiz_shared:
    path: ../quiz_shared/

dev_dependencies:
  http: ^0.13.0
  lints: ^1.0.0
  # test_process: ^2.0.0
  # test: ^1.15.0
